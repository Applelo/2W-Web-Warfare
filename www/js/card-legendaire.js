/*svg: insère une forme svg*/
function HTMLsvg() {
  document.getElementById("outer-dropzone").innerHTML = document.getElementById("outer-dropzone").innerHTML + "<svg width='100' height='100'><circle cx='50' cy='50' r='40' stroke='green' stroke-width='4' fill='yellow' /></svg>";
  return score += 3;
}
var numBits = "";
for (var i = 0; i < 30; i++) {
for (var j = 0; j < 10; j++) {
  numBits =  numBits + "0101010011010101";
}
numBits = numBits + "<br>";
}

/* bits : remplace toute la page par des 0 et 1 */
function LegendaireBits() {
  document.getElementById("outer-dropzone").innerHTML = "<p>" +  numBits + "</p>" ;
  return score += 3;
}
